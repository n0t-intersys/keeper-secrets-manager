# Python
__pycache__/
*.pyc
*.pyo

# Build artifacts from `pip install -e .`
*.egg-info/
build/
dist/

# Installer build outputs (downloaded Python + compiled .exe)
installer/vendor/
installer/Output/
*.exe

# KSM config dumps / secrets — must never be committed
keeper.ini
*config*.json
ksm-config.json
.env
.env.*

# OS noise
Thumbs.db
.DS_Store

# Local Claude Code settings (per-user, not for the team repo)
.claude/
