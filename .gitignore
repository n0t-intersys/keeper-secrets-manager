# Python
__pycache__/
*.pyc
*.pyo

# Build artifacts from `pip install -e .`
*.egg-info/
build/
dist/

# KSM config dumps / secrets — must never be committed
keeper.ini
*config*.json
ksm-config.json
.env
.env.*
# ...but DO track the template (it holds references, not secrets)
!.env.template
!.env.example

# OS noise
Thumbs.db
.DS_Store

# Local Claude Code settings (per-user, not for the team repo)
.claude/
